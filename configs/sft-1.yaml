model_name: "meta-llama/Llama-3.2-1B"
dataset: "tulu3"
 
# Training
learning_rate: 0.0005
lr_schedule: "linear"
num_epochs: 1
# LoRA
lora_rank: 16

# Batching
batch_size: 128
max_length: 4096

# Checkpointing / logging
save_every: 500
eval_every: 500
log_path: "logs/sft-full-1"